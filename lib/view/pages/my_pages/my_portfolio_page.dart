import 'package:flutter/material.dart';

class MyPortfolioPage extends StatelessWidget {
  const MyPortfolioPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('My PortFolio Page'),
      ),
    );
  }
}
