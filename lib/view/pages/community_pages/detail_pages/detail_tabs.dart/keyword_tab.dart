import 'package:flutter/material.dart';

class KeywordTab extends StatelessWidget {
  const KeywordTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
